# Case Study Template

This directory is the canonical scaffold for future case studies: keep the
dataset-selection method, txid list, local pipeline, presentation, and query
evidence in separate files so readers can reproduce the lattice and audit
each numeric claim.
