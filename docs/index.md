# cardano-rdf

`cardano-rdf` is the generic Cardano RDF home for deterministic graph
extraction, vocabulary documentation, packaged views, and export tooling.

The initial migration extracts the transaction RDF surface from
`cardano-tx-tools` without deleting the old source.
