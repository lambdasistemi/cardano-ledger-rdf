# tx-rdf-core

Lean pure library package for Cardano Conway transaction RDF emission,
serialisation, in-memory Turtle rule parsing, and packaged view rendering.

The `cardano-ledger-rdf` package keeps CLI, network, YAML, and filesystem
integration.
